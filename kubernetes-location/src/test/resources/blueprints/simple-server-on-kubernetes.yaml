location:
  kubernetes:
    endpoint: "http://10.101.0.236:8080/"
    # default `image` is tutum/ubuntu

services:
- serviceType: org.apache.brooklyn.entity.software.base.EmptySoftwareProcess
  name: Server
  env: { ROOT_PASS : password }


